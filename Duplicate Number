class Solution:
    def findDuplicate(self, nums: List[int]) -> int:
        seen=set()
        for j in nums:
            if j not in seen:
                seen.add(j)
            else:
                return j
